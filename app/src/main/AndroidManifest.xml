<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.parteek.fysquefreak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Excersice" />
        <activity android:name=".Biceps" />
        <activity android:name=".Chest" />
        <activity android:name=".BenchPress" />
        <activity android:name=".InclineBench" />
        <activity android:name=".InclinedDumbell" />
        <activity android:name=".ParallelDips"></activity>
    </application>

</manifest>